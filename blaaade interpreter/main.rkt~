#lang racket
(require "utils.rkt")
(require "parser-v2.rkt")
(require "interpreter-v2.rkt")

(define var-env
  '(((a 1) (b 2) (x 5)))
  )

(define execute
  (lambda (code)
    (blaaade-interpreter (blaaade-parser code) var-env)
    )
  )

;(program (num-exp 1))
(blaaade-parser '(program (1)))


(blaaade-2-interpreter (blaaade-parser '(queue (put a = 3) (out a))) var-env)
